// Auto-generated quiz data from z.md
const RAW_MD = `# JAVA编程语言题库

> 共提取 194 道题，来源：z.pdf

## 1. 下面那种类型不属于Java的基本数据类型（ ）?

- A. int
- B. String
- C. char
- D. boolean

**答案：** B

## 2. 以下运算符中，优先级最高的是？

- A. +（加法）
- B. *（乘法）
- C. ()（括号）
- D. ==（等于）

**答案：** C

**解析：** Java运算符优先级中，括号()的优先级最高，用于改变运算顺序。乘法*优先级高于加法+，等于==优先级低于算术运算符。

## 3. 以下哪个语句可以正确声明一个长度为5的int型数组？

- A. int[] arr = new int[5];
- B. int arr[] = new int(5);
- C. int arr[5];
- D. int[5] arr;

**答案：** A

**解析：** Java数组声明的正确语法为：数据类型[]数组名 = new数据类型[长度];或数据类型数组名[]= new数据类型[长度];选项B使用圆括号错误，选项C、D声明方式不符合Java语法规范。

## 4. 以下关于类的描述，正确的是？

- A. 一个类只能有一个构造方法
- B. 类中的成员变量默认是private访问权限
- C. 类是对象的模板，对象是类的实例
- D. 类不能包含静态方法

**答案：** C

**解析：** 类是对象的抽象模板，对象是类的具体实例；一个类可以有多个构造方法（方法重载）；成员变量默认访问权限是package-private（包访问权限）；类可以包含静态方法（用static修饰）。

## 5. 以下哪个访问修饰符的可见范围最小？

- A. public
- B. protected
- C. default（无修饰符）
- D. private

**答案：** D

**解析：** Java访问修饰符的可见范围从大到小为：public（所有类可见）> protected（同包或子类可见）> default（同包可见）> private（仅本类可见）。

## 6. 以下哪个关键字用于引用当前对象的实例？

- A. super
- B. this
- C. static
- D. final

**答案：** B

**解析：** this关键字用于引用当前对象的实例，可以访问当前对象的成员变量和方法；super用于访问父类的成员；static用于修饰静态成员；final用于修饰不可变的变量、方法或类。

## 7. 以下关于方法重载的描述，正确的是？

- A. 方法重载要求方法名不同
- B. 方法重载要求参数列表不同
- C. 方法重载要求返回值类型不同
- D. 方法重载要求访问修饰符不同

**答案：** B

**解析：** 方法重载的条件是：方法名相同，参数列表（参数个数、类型、顺序）不同；返回值类型和访问修饰符不影响方法重载。

## 8. 以下哪个是构造方法的特点？

- A. 有返回值类型
- B. 方法名与类名相同
- C. 可以用static修饰
- D. 可以被直接调用

**答案：** B

**解析：** 构造方法的特点：方法名与类名相同；没有返回值类型（包括void）；不能用static、final、abstract等修饰；只能通过new关键字调用。

## 9. 以下关于继承的描述，正确的是？

- A. Java支持多继承
- B. 子类可以继承父类的所有成员
- C. 子类可以重写父类的方法
- D. 父类可以继承子类的成员

**答案：** C

**解析：** Java只支持单继承（一个子类只能有一个直接父类）；子类不能继承父类的private成员；子类可以通过重写（override）改变父类方法的实现；继承是子类继承父类，父类不能继承子类。

## 10. 以下哪个异常属于运行时异常？

- A. IOException
- B. NullPointerException
- C. ClassNotFoundException
- D. SQLException

**答案：** B

**解析：** 运行时异常（RuntimeException）是未检查异常，包括NullPointerException、ArrayIndexOutOfBoundsException等；IOException、ClassNotFoundException、SQLException属于受检查异常（编译时异常）。

## 11. 以下关于try-catch语法的描述，正确的是？

- A. try块可以没有catch块
- B. catch块可以捕获所有类型的异常
- C. 一个try块可以有多个catch块
- D. finally块在异常发生时才会执行

**答案：** C

**解析：** try块必须有catch块或finally块；catch块需要指定异常类型，不能捕获所有异常（除非用Exception）；一个try块可以有多个catch块处理不同异常；finally块无论是否发生异常都会执行。

## 12. 以下哪个语句可以正确创建String对象？

- A. String s = 'hello';
- B. String s = new String("hello");
- C. String s = new String(123);
- D. String s = hello;

**答案：** B

**解析：** String是引用类型，创建对象可以用双引号直接赋值（String s = "hello"）或通过new关键字（String s = new String("hello")）；单引号用于char类型；选项C参数类型错误；选项D未加引号会被当作变量。

## 13. 以下哪个循环语句至少执行一次循环体？

- A. for循环
- B. while循环
- C. do-while循环
- D. foreach循环

**答案：** C

**解析：** do-while循环先执行循环体，再判断条件，因此至少执行一次；for、while、foreach循环先判断条件，条件不满足则不执行循环体。

## 14. 以下关于成员变量初始化的描述，正确的是？

- A. 成员变量会被自动初始化为默认值
- B. 成员变量必须手动初始化
- C. 成员变量的默认值都是null
- D. 局部变量会被自动初始化为默认值

**答案：** A

**解析：** Java中成员变量（类的属性）会被自动初始化为默认值（如int为0，boolean为false，引用类型为null）；局部变量（方法内的变量）需要手动初始化，否则编译报错。

## 15. 以下哪个关键字用于实现多态？

- A. final
- B. static
- C. override
- D. extends
- E. 以上都不是（正确为方法重写+向上转型）

**答案：** E

**解析：** 多态的实现依赖于方法重写（override）和向上转型（父类引用指向子类对象），选项中没有直接对应关键字，因此选E。

## 16. 以下哪个类型变换是安全的？

- A. int类型转换为byte类型
- B. double类型转换为int类型
- C. 子类对象转换为父类类型
- D. 父类对象转换为子类类型

**答案：** C

**解析：** 向上转型（子类转父类）是安全的，属于自动类型转换；向下转型（父类转子类）需要强制转换且可能不安全；int转byte、double转int属于窄化转换，可能丢失精度。

## 17. 以下哪个运算符是短路与运算符？

- A. &
- B. &&
- C. |
- D. ||

**答案：** B

**解析：** &&是短路与运算符，当第一个条件为false时，第二个条件不再执行；&是逻辑与运算符，无论第一个条件是否为false，都会执行第二个条件；||是短路或运算符；|是逻辑或运算符。

## 18. 以下关于对象创建的描述，正确的是？

- A. 创建对象不需要使用new关键字
- B. 一个类只能创建一个对象
- C. 创建对象时会调用构造方法
- D. 对象创建后不能修改其成员变量

**答案：** C

**解析：** Java中创建对象必须使用new关键字；一个类可以创建多个对象；创建对象时会调用对应的构造方法初始化对象；对象的成员变量可以通过setter方法或直接访问（若权限允许）修改。

## 19. 以下哪个方法是String类的常用方法？

- A. add()
- B. length()
- C. remove()
- D. put()

**答案：** B

**解析：** String类的常用方法包括length()（获取长度）、substring()（截取子串）、equals()（比较内容）等；add()是集合类的方法；remove()是集合或字符串缓冲区的方法；put()是Map类的方法。

## 20. 以下哪个语句可以正确遍历int数组arr？


**答案：** 

**解析：** 遍历数组可以用普通for循环（通过索引访问）或foreach循环（直接遍历元素），选项A和C的写法均正确。

## 21. Java中的byte数据类型占1个字节，取值范围是-128到127。


**答案：** 正确

**解析：** Java的基本数据类型中，byte类型长度为8位（1字节），有符号，因此取值范围是-2^7到2^7-1，即-128到127。

## 22. 在Java中，'+'运算符只能用于数值的加法运算，不能用于字符串拼接。


**答案：** 错误

**解析：** Java中'+'运算符具有双重功能：当操作数均为数值时执行加法运算；当其中一个操作数为字符串时，执行字符串拼接操作。

## 23. while循环的循环体至少会执行一次，而do-while循环的循环体可能一次都不执行。


**答案：** 错误

**解析：** while循环先判断条件再执行循环体，条件不满足时循环体不执行；do-while循环先执行循环体再判断条件，因此循环体至少执行一次。

## 24. Java数组的长度一旦确定就无法修改，且数组元素的默认值与数据类型相关（如int数组默认值为0，引用类型数组默认值为null）。


**答案：** 正确

**解析：** Java数组是定长容器，长度由初始化时确定；基本数据类型数组元素默认值为对应类型的零值，引用类型数组元素默认值为null。

## 25. Java中类的私有成员（private修饰）可以被同一个包中的其他类直接访问。


**答案：** 错误

**解析：** private访问修饰符限制成员只能在当前类内部被访问，同一个包中的其他类无法直接访问。

## 26. 方法重载要求方法名相同，参数列表（参数个数、类型或顺序）不同，与返回值类型无关。


**答案：** 正确

**解析：** 方法重载的核心条件是方法名相同且参数列表不同，返回值类型不影响重载的判定。

## 27. 子类构造方法必须通过super()调用父类的构造方法，且super()必须是子类构造方法的第一条语句。


**答案：** 正确

**解析：** 子类构造时需先初始化父类部分，super()用于调用父类构造方法，且必须放在子类构造方法的第一行。

## 28. this关键字可以在静态方法中使用，用于指代当前类的实例对象。


**答案：** 错误

**解析：** 静态方法属于类而非实例，没有this指针，因此不能在静态方法中使用this关键字。

## 29. 向上转型（父类引用指向子类对象）是安全的，不需要显式类型转换；向下转型则需要显式转换，且可能抛出ClassCastException。


**答案：** 正确

**解析：** 向上转型是自动类型转换，符合继承的is-a关系；向下转型是强制类型转换，需确保引用实际指向的是子类对象，否则会抛出异常。

## 30. try-catch语句中，catch块可以捕获所有类型的异常，包括Error和RuntimeException。


**答案：** 正确

**解析：** catch块可以通过Exception或Throwable类型捕获所有异常（包括RuntimeException）和错误（Error），但通常不建议捕获Error，因为它属于系统级错误，程序难以恢复。

## 31. 请详细描述Java中基本数据类型与引用数据类型的区别，并举例说明各自的内存分配机制。


**答案：** 1.存储方式：基本数据类型直接存储值，存储在栈内存中；引用数据类型存储对象的引用（内存地址），对象本身存储在堆内存中。 2.初始值：基本数据类型有默认初始值（如int默认0，boolean默认false）；引用数据类型默认初始值为null。 3.传递方式：基本数据类型按值传递，方法参数接收的是值的副本；引用数据类型按引用传递，方法参数接收的是对象引用的副本，操作会影响原对象。 举例：int a =10;变量a直接存储10在栈中；String s = new String("java");变量s存储堆中字符串对象的地址，对象内容"java"在堆中。

**解析：** 本题考察基本数据类型与引用数据类型的核心区别，需从存储、初始值、传递方式三个维度分析，同时结合内存分配机制的示例加深理解，是Java基础中的关键概念。

## 32. 请解释Java中的方法重载（Method Overloading）和方法重写（Method Overriding）的区别，并分别说明其实现条件与应用场景。


**答案：** 1.方法重载（Overloading）： -定义：在同一个类中，方法名相同但参数列表（参数个数、类型、顺序）不同的方法。 -实现条件：方法名相同，参数列表不同（与返回值类型、访问修饰符无关）。 -应用场景：实现同一功能的不同参数形式，如System.out.println()支持多种数据类型的输出。 2.方法重写（Overriding）： -定义：子类重新实现父类中已有的方法，方法名、参数列表、返回值类型（协变返回类型除外）完全一致。 -实现条件：子类继承父类，方法签名（方法名+参数列表）与父类相同，访问修饰符不能比父类更严格，不能抛出比父类更多的 checked异常。 -应用场景：实现多态，子类根据自身需求修改父类方法的逻辑，如Animal类的eat()方法在Dog子类中重写为"吃骨头"。

**解析：** 本题考察方法重载与重写的核心差异，需明确两者的定义、实现条件及应用场景，是理解Java多态特性的重要基础，需区分编译时多态（重载）与运行时多态（重写）的本质区别。

## 33. 5.JAVA语言里异常处理机制有些关键字，有什么作用。


**答案：** Try 语句块用于包含可能抛出异常的语句块。Catch语句块用于 捕获异常。Throws ,用于在函数后面声明抛出异常。Throw 用于抛出一个异常对象。Finally 可和try联用，必须要执行。

## 34. public static void bubbleSort(int[] arr) {int n = arr.length;for (int i = 0; i < __________; i++) {for (int j = 0; j < __________; j++) {// 比较相邻元素if (arr[j] __________ arr[j + 1]) {// 交换元素int temp = __________;__________ = arr[j + 1];arr[j + 1] = temp;}}}}


**答案：** n;n-1；n;n-i;n-1-i；>；arr[j]；arr[j]；

## 35. 下面哪个选项是正确的关于遍历数组的语法()。

- A. for(inti=0;i<arr.length;i++){}
- B. for(inti=0;i<=arr.length;i++){}
- C. for(inti=1;i<=arr.length;i++){}
- D. for(inti=1;i<arr.length;i++){}

**答案：** A

## 36. 在Java中，以下哪个不是基本数据类型？

- A. int
- B. String
- C. boolean
- D. double

**答案：** B

**解析：** Java的基本数据类型包括byte、short、int、long、float、double、char、boolean，共8种。String属于引用数据类型，不是基本数据类型。

## 37. 以下运算符中，优先级最高的是哪个？

- A. +（加法）
- B. *（乘法）
- C. ()（括号）
- D. ==（等于）

**答案：** C

**解析：** Java中运算符的优先级从高到低依次为：括号()、一元运算符（如++、--）、算术运算符（*、/、%高于+、-）、关系运算符、逻辑运算符、赋值运算符。括号的优先级最高，用于改变运算顺序。

## 38. 以下哪个语句可以正确声明一个长度为5的int型数组？

- A. int[] arr = new int[5];
- B. int arr[] = new int[5];
- C. int arr = new int[5];
- D. int[] arr = new int[];

**答案：** A

**解析：** Java中声明数组的正确方式有两种：int[] arr或 int arr[]，但初始化时必须指定长度（如new int[5]）或直接赋值（如new int[]{1,2,3}）。选项B的声明方式虽然语法允许，但通常推荐使用int[] arr的形式；选项C缺少数组标识[]；选项D初始化时未指定长度，语法错误。

## 39. String类的哪个方法可以返回字符串的长度？

- A. length()
- B. size()
- C. getLength()
- D. length

**答案：** A

**解析：** String类的length()方法用于返回字符串的字符个数；size()方法通常用于集合类（如List）；getLength()不是String类的方法；length是数组的属性，不是String的方法。

## 40. 以下关于类与对象的描述，正确的是？

- A. 类是对象的实例
- B. 对象是类的模板
- C. 一个类可以创建多个对象
- D. 对象不能调用类的方法

**答案：** C

**解析：** 类是对象的模板，描述了对象的属性和行为；对象是类的实例，一个类可以创建多个对象。对象可以通过“对象名.方法名()”的方式调用类的方法。

## 41. Java中访问控制修饰符中，权限从大到小依次为？

- A. public > protected > default > private
- B. public > default > protected > private
- C. private > protected > default > public
- D. protected > public > default > private

**答案：** A

**解析：** Java的访问控制修饰符权限从大到小为：public（所有类可访问）、protected（同一包或子类可访问）、default（同一包可访问，无修饰符）、private（仅本类可访问）。

## 42. 以下哪个关键字用于创建对象？

- A. class
- B. new
- C. this
- D. static

**答案：** B

**解析：** class用于定义类；new用于创建对象，分配内存并调用构造方法；this用于引用当前对象；static用于修饰类成员（静态变量、静态方法）。

## 43. 方法重载的条件不包括以下哪项？

- A. 方法名相同
- B. 参数列表不同（个数、类型、顺序）
- C. 返回值类型不同
- D. 可以在同一个类或子类中

**答案：** C

**解析：** 方法重载的条件是：方法名相同，参数列表（个数、类型、顺序）不同。返回值类型不同不能作为方法重载的判断条件，因为Java中方法的区分不依赖返回值。

## 44. this关键字的作用不包括以下哪项？

- A. 引用当前对象
- B. 调用当前对象的构造方法
- C. 区分成员变量和局部变量
- D. 调用父类的方法

**答案：** D

**解析：** this关键字用于引用当前对象、调用当前对象的构造方法（this()）、区分成员变量和局部变量（如this.name = name）。调用父类的方法使用super关键字，而非this。

## 45. 以下关于构造方法的描述，正确的是？

- A. 构造方法可以有返回值
- B. 构造方法的名字必须与类名相同
- C. 一个类只能有一个构造方法
- D. 构造方法不能被重载

**答案：** B

**解析：** 构造方法的名字必须与类名相同，没有返回值（连void都不能写）；一个类可以有多个构造方法（构造方法重载）；构造方法可以被重载以满足不同的对象初始化需求。

## 46. 成员变量在创建对象时的默认初始化值，以下错误的是？

- A. int型默认值为0
- B. boolean型默认值为false
- C. 引用类型默认值为null
- D. char型默认值为' '（空格）

**答案：** D

**解析：** 成员变量的默认初始化值：数值类型（byte、short、int、long、float、double）默认值为0或0.0；boolean默认值为false；char默认值为''（空字符，不是空格）；引用类型默认值为null。

## 47. 以下关于局部变量的描述，正确的是？

- A. 局部变量可以用访问控制修饰符修饰
- B. 局部变量在声明时必须初始化
- C. 局部变量的作用域是整个类
- D. 局部变量存储在堆内存中

**答案：** B

**解析：** 局部变量是在方法、代码块或构造方法中声明的变量，不能用访问控制修饰符修饰；必须在声明时初始化，否则编译错误；作用域仅限于声明它的代码块内；存储在栈内存中（成员变量存储在堆内存）。

## 48. 以下哪个是创建对象的正确步骤？

- A. 定义类 →声明对象 →实例化对象
- B. 声明对象 →定义类 →实例化对象
- C. 实例化对象 →定义类 →声明对象
- D. 定义类 →实例化对象 →声明对象

**答案：** A

**解析：** 创建对象的正确流程是：首先定义类（描述对象的属性和行为），然后声明对象（如ClassName obj;），最后通过new关键字实例化对象（如obj = new ClassName();）。

## 49. 以下关于继承的描述，正确的是？

- A. Java支持多继承
- B. 子类可以继承父类的所有成员
- C. 子类可以重写父类的方法
- D. 父类可以访问子类的成员

**答案：** C

**解析：** Java只支持单继承（一个子类只能有一个直接父类）；子类不能继承父类的private成员；子类可以通过重写（override）改变父类方法的实现；父类不能直接访问子类的成员，除非通过子类对象。

## 50. 以下哪个是向上转型的正确示例？（假设Animal是父类，Dog是子类）

- A. Animal a = new Dog();
- B. Dog d = new Animal();
- C. Animal a = (Animal) new Dog();
- D. Dog d = (Dog) new Animal();

**答案：** A

**解析：** 向上转型是指将子类对象赋值给父类引用，语法为“父类类型引用名 = new子类类型();”，不需要强制转换。选项B是向下转型（父类对象赋值给子类引用），需要强制转换且可能引发ClassCastException；选项C的强制转换是多余的；选项D会引发异常。

## 51. instanceof运算符的作用是？

- A. 判断一个对象是否属于某个类或其子类的实例
- B. 判断两个对象是否相等
- C. 判断一个变量是否为null
- D. 判断一个方法是否被重写

**答案：** A

**解析：** instanceof运算符用于判断一个对象是否是某个类（或其子类、接口）的实例，返回boolean类型。判断对象相等用equals()方法；判断变量是否为null用“== null”；判断方法是否被重写需要通过反射或运行时行为分析。

## 52. 以下哪个属于受检异常（Checked Exception）？

- A. NullPointerException
- B. ArrayIndexOutOfBoundsException
- C. IOException
- D. ArithmeticException

**答案：** C

**解析：** 受检异常是编译时必须处理的异常，如IOException、SQLException等；非受检异常（RuntimeException及其子类）是运行时异常，如NullPointerException、ArrayIndexOutOfBoundsException、ArithmeticException等，编译时不需要强制处理。

## 53. 以下哪个关键字用于捕获异常？

- A. try
- B. catch
- C. finally
- D. throw

**答案：** B

**解析：** try块用于包裹可能抛出异常的代码；catch块用于捕获并处理异常；finally块用于执行无论是否发生异常都必须执行的代码；throw用于手动抛出异常。

## 54. 以下关于多态的描述，错误的是？

- A. 多态的实现依赖于继承和方法重写
- B. 多态可以提高代码的可扩展性
- C. 多态时，父类引用只能调用父类中定义的方法
- D. 多态分为编译时多态和运行时多态

**答案：** C

**解析：** 多态时，父类引用可以调用子类重写后的方法（运行时多态），而不是只能调用父类方法。编译时多态通过方法重载实现，运行时多态通过方法重写和向上转型实现。

## 55. 以下哪个不是Java的编程概念？

- A. 面向对象
- B. 跨平台
- C. 指针操作
- D. 垃圾回收

**答案：** C

**解析：** Java是面向对象语言，支持跨平台（通过JVM），具有自动垃圾回收机制；但Java不支持指针操作，以避免内存安全问题。

## 56. 在Java中，byte类型的取值范围是-128到127（包含两端）。


**答案：** 正确

**解析：** Java的byte类型是8位有符号整数，其取值范围为-2^7到2^7-1，即-128到127。

## 57. Java中的算术运算符'/'对两个整数进行运算时，结果会自动保留小数部分。


**答案：** 错误

**解析：** Java中整数除法会舍弃小数部分，只保留整数商，例如5/2的结果是2而非2.5。

## 58. while循环的循环体至少会执行一次。


**答案：** 错误

**解析：** while循环先判断条件，若条件初始为false，则循环体一次都不会执行；do-while循环才会至少执行一次。

## 59. Java数组的长度一旦确定就无法修改。


**答案：** 正确

**解析：** Java数组是定长结构，创建时指定长度后，其大小不能动态改变，若需可变长度集合需使用ArrayList等容器。

## 60. String类的对象是不可变的，修改字符串内容会创建新的String对象。


**答案：** 正确

**解析：** String类被设计为不可变类，任何对字符串的修改操作（如concat、replace）都会生成新的String实例，原对象内容不变。

## 61. 面向对象编程的三大特征是封装、继承和多态。


**答案：** 正确

**解析：** 封装隐藏对象内部细节，继承实现代码复用，多态允许同一行为有不同表现形式，三者是面向对象的核心特征。

## 62. Java中一个类可以继承多个父类。


**答案：** 错误

**解析：** Java是单继承语言，一个类只能直接继承一个父类，但可通过接口实现多继承的效果。

## 63. 被private修饰的成员变量只能在本类内部访问。


**答案：** 正确

**解析：** private是Java中最严格的访问修饰符，其修饰的成员仅对当前类可见，子类和其他类无法直接访问。

## 64. 使用new关键字创建对象时，会调用类的构造方法。


**答案：** 正确

**解析：** new运算符用于实例化对象，其本质是分配内存并调用对应构造方法初始化对象。

## 65. 方法重载要求方法的参数个数、类型或顺序必须不同。


**答案：** 正确

**解析：** 方法重载的核心是方法签名（方法名+参数列表）不同，参数的个数、类型或顺序变化都会导致签名不同。

## 66. this关键字可以在静态方法中使用。


**答案：** 错误

**解析：** this代表当前对象的引用，而静态方法属于类而非对象，因此静态方法中无法使用this。

## 67. 如果一个类没有显式定义构造方法，Java会提供一个默认的无参构造方法。


**答案：** 正确

**解析：** 当类未定义任何构造方法时，编译器会自动生成一个无参、无方法体的默认构造方法。

## 68. 成员变量如果没有显式初始化，会被Java自动赋予默认值（如int默认0，boolean默认false）。


**答案：** 正确

**解析：** 类的成员变量属于对象状态，Java会为其分配默认初始值；局部变量则需显式初始化才能使用。

## 69. 局部变量可以被private修饰。


**答案：** 错误

**解析：** 访问修饰符（private、public等）仅适用于类的成员（成员变量、成员方法），局部变量不能使用访问修饰符。

## 70. 子类对象创建时，会先调用父类的构造方法，再调用子类的构造方法。


**答案：** 正确

**解析：** 子类构造方法默认会隐式调用父类的无参构造方法（通过super()），确保父类成员先初始化。

## 71. 向上转型是将子类对象赋值给父类引用，该操作是安全的且不需要强制类型转换。


**答案：** 正确

**解析：** 向上转型符合“is-a”关系，例如Dog是Animal的子类，Dog对象可以直接赋值给Animal引用，无需强制转换。

## 72. instanceof运算符用于判断一个对象是否是某个类（或其子类、接口）的实例。


**答案：** 正确

**解析：** instanceof的语法为“对象 instanceof类/接口”，返回boolean值，可用于多态场景下的类型判断。

## 73. NullPointerException属于编译时异常，必须在代码中显式处理。


**答案：** 错误

**解析：** NullPointerException是运行时异常（非受检异常），编译器不强制要求处理，通常由程序逻辑错误导致。

## 74. try-catch-finally结构中，finally块无论是否发生异常都会执行。


**答案：** 正确

**解析：** finally块用于释放资源等收尾操作，即使try或catch块中存在return语句，finally块仍会执行（除非JVM退出）。

## 75. 成员变量和局部变量同名时，在方法内部使用this关键字可以访问成员变量。


**答案：** 正确

**解析：** 当局部变量遮蔽成员变量时，this.成员变量名可以明确指向类的成员变量，区分两者的作用域。

## 76. 在Java中，基本数据类型和引用数据类型的本质区别是什么？请分别列举至少3种基本数据类型和2种引用数据类型。


**答案：** 基本数据类型直接存储值，占用固定内存空间；引用数据类型存储对象的引用（内存地址），指向堆内存中的对象。基本数据类型示例：int、double、boolean；引用数据类型示例：String、ArrayList。

**解析：** 本题考察对Java数据类型分类的理解，需明确两类数据类型的存储方式差异及常见实例。

## 77. 请解释Java中的算术运算符%的作用，并举例说明其在正数和负数运算中的结果规则。


**答案：** %是取模运算符，用于计算两个数相除的余数。规则：结果的符号与被除数一致。示例：5%2=1，-5%2=-1，5%-2=1，-5%-2=-1。

**解析：** 考察取模运算符的功能及符号规则，需注意余数符号与被除数的关联。

## 78. 简述Java中if-else语句与switch语句的适用场景差异。


**答案：** if-else适用于判断条件为布尔表达式（如范围判断、复杂逻辑组合）的场景；switch适用于判断一个变量与多个固定值匹配的场景（支持byte、short、int、char及对应的包装类、String、enum）。

**解析：** 对比两种分支语句的适用场景，需明确各自的语法特点和使用限制。

## 79. Java中一维数组的初始化方式有哪两种？请分别写出示例代码。


**答案：** 静态初始化：直接指定数组元素，语法如int[] arr = {1,2,3};动态初始化：指定数组长度，语法如int[] arr = new int[3];（元素默认初始化为对应类型的默认值）。

**解析：** 考察数组的两种初始化方式，需掌握静态与动态初始化的语法格式。

## 80. String类的本质是什么？为什么说String对象是不可变的？


**答案：** String类是final修饰的，其内部用char数组（JDK8及以前）或byte数组（JDK9+）存储字符，且数组被final修饰。不可变指一旦创建String对象，其内容无法修改，修改操作会生成新的String对象。

**解析：** 考察String类的不可变性特性，需理解其内部存储结构和final修饰的作用。

## 81. 请解释Java中的面向对象三大特征，并简要说明每个特征的核心思想。


**答案：** 封装：将对象的属性和行为封装为类，通过访问控制符隐藏内部细节，仅暴露必要接口；继承：子类继承父类的属性和方法，实现代码复用和扩展；多态：同一方法在不同对象上有不同实现，包括编译时多态（方法重载）和运行时多态（方法重写）。

**解析：** 考察面向对象的核心特征，需掌握封装、继承、多态的基本概念。

## 82. Java中的访问控制符有哪些？请按权限从大到小排列，并说明各自的访问范围。


**答案：** 访问控制符包括public、protected、default（无修饰符）、private。权限从大到小：public（所有类可访问）> protected（本类、同包、子类）> default（本类、同包）> private（仅本类）。

**解析：** 考察访问控制符的种类及权限范围，需准确记忆不同修饰符的访问规则。

## 83. Java中创建对象的步骤是什么？请用代码示例说明。


**答案：** 步骤：1.加载类（类加载器完成）；2.分配内存；3.初始化实例变量（默认值或显式初始化）；4.调用构造方法。示例：class Person{}; Person p = new Person();

**解析：** 考察对象创建的过程，需理解类加载、内存分配、初始化及构造方法调用的顺序。

## 84. 什么是方法重载？方法重载的条件有哪些？


**答案：** 方法重载指在同一个类中，多个方法具有相同的方法名，但参数列表（参数个数、类型、顺序）不同。条件：方法名相同；参数列表不同；与返回值类型和访问修饰符无关。

**解析：** 考察方法重载的定义和条件，需明确参数列表是重载的核心判断依据。

## 85. this关键字在Java中的主要作用是什么？请列举至少3种使用场景。


**答案：** this代表当前对象的引用。使用场景：1.区分成员变量和局部变量（如this.name =name）；2.调用本类的其他构造方法（this(参数)，必须放在构造方法第一行）；3. 返回当前对象（return this）。

**解析：** 考察this关键字的功能及常见使用场景，需掌握其在变量区分、构造方法调用等场景的应用。

## 86. 构造方法的作用是什么？它有哪些特点？


**答案：** 构造方法用于创建对象时初始化对象。特点：1.方法名与类名相同；2.无返回值（连void都没有）；3. 创建对象时由JVM自动调用；4.若类中未定义构造方法，编译器会生成默认无参构造方法。

**解析：** 考察构造方法的功能和语法特点，需明确其与普通方法的区别。

## 87. Java中成员变量和局部变量的初始化规则有什么不同？


**答案：** 成员变量：类的成员变量会被JVM自动初始化（默认值，如int为0，Object为null）；局部变量：方法或代码块中的局部变量必须显式初始化，否则编译报错。

**解析：** 对比成员变量和局部变量的初始化规则，需注意局部变量无默认初始化的特点。

## 88. 请解释Java中的继承关系，并说明子类如何访问父类的成员？


**答案：** 继承通过extends关键字实现，子类继承父类的非private成员。子类访问父类成员：1.直接访问（非private且无重写）；2. 使用super关键字访问（如super.method()访问父类方法，super.name访问父类成员变量）。

**解析：** 考察继承的基本概念及子类访问父类成员的方式，需掌握super关键字的使用。

## 89. 什么是向上转型和向下转型？向下转型时需要注意什么？


**答案：** 向上转型：将子类对象赋值给父类引用（如Parent p = new Child();），是自动类型转换；向下转型：将父类引用转换为子类类型（如Child c = (Child)p;），需强制类型转换，且需确保父类引用实际指向子类对象，否则会抛出ClassCastException。

**解析：** 考察类型变换的两种形式，需明确向下转型的风险及注意事项。

## 90. instanceof运算符的作用是什么？请举例说明其使用场景。


**答案：** instanceof用于判断一个对象是否是某个类（或其子类、接口实现类）的实例。示例：if(p instanceof Person) { ... }，可用于向下转型前的类型检查，避免ClassCastException。

**解析：** 考察instanceof运算符的功能及应用场景，需掌握其在类型判断中的作用。

## 91. Java中的异常分为哪两大类？请分别列举至少2种常见异常。


**答案：** 异常分为Checked Exception（编译时异常）和Unchecked Exception（运行时异常）。Checked Exception示例：IOException、SQLException；Unchecked Exception示例：NullPointerException、ArrayIndexOutOfBoundsException。

**解析：** 考察异常的分类及常见实例，需区分编译时和运行时异常的差异。

## 92. Java中处理异常的机制有哪些？请简要说明try-catch-finally的执行流程。


**答案：** 处理机制：try-catch捕获异常、throws声明异常、finally释放资源。try-catch-finally流程：执行try块代码，若发生异常则匹配catch块处理，无论是否发生异常，finally块都会执行（除非JVM退出）。

**解析：** 考察异常处理的基本机制及try-catch-finally的执行顺序，需理解finally块的执行特性。

## 93. 请解释Java中的“封装”思想，并说明如何通过访问控制符实现封装。


**答案：** 封装是将对象的属性和行为隐藏在类内部，仅通过公共方法对外暴露接口。实现方式：用private修饰成员变量，提供public的getter和setter方法访问和修改变量。

**解析：** 考察封装的概念及实现方式，需掌握private修饰符和getter/setter方法的作用。

## 94. 什么是多态？Java中实现多态的必要条件是什么？


**答案：** 多态指同一行为在不同对象上有不同表现形式。必要条件：1.继承关系；2.方法重写；3.父类引用指向子类对象（向上转型）。

**解析：** 考察多态的定义及实现条件，需明确继承、重写和向上转型三者的关联。

## 95. Java中局部变量和成员变量的作用域有什么不同？


**答案：** 局部变量的作用域是定义它的方法、代码块或构造方法内部；成员变量的作用域是整个类（若为实例变量）或整个类及所有对象（若为类变量static修饰）。

**解析：** 对比局部变量和成员变量的作用域范围，需明确两者的有效范围差异。

## 96. 在Java中，以下关于访问控制修饰符的描述，错误的是哪一项？

- A. public修饰的成员可以被所有类访问，包括不同包中的非子类
- B. protected修饰的成员可以被同一包中的所有类访问，以及不同包中的子类访问
- C. 默认（包访问权限）修饰的成员只能被同一包中的类访问，不同包中的子类也无法访问
- D. private修饰的成员可以被同一类中的其他方法访问，也可以被同一包中的其他类访问

**答案：** D

**解析：** private修饰的成员只能被**同一类**中的方法访问，同一包中的其他类无法访问该成员。其他选项对访问控制修饰符的描述均正确。

## 97. 以下关于Java构造方法的说法，正确的是哪一项？

- A. 构造方法必须有返回值类型，且返回值类型为void
- B. 一个类可以有多个构造方法，它们之间构成方法重载
- C. 构造方法的名称必须与类名不同
- D. 如果一个类没有显式定义构造方法，Java编译器不会自动生成默认构造方法

**答案：** B

**解析：** 构造方法没有返回值类型（连void都不能写），名称必须与类名相同；若类未显式定义构造方法，编译器会生成无参默认构造方法。方法重载要求方法名相同、参数列表不同，多个构造方法满足此条件，因此构成方法重载。

## 98. 在Java中，以下代码片段的运行结果是？ public class Test { public static void main(String[] args) { int a =10; int b =5; System.out.println(a++ + ++b); } }

- A. 15
- B. 16
- C. 17
- D. 18

**答案：** B

**解析：** a++是后置自增，先参与运算（值为10）再自增；++b是前置自增，先自增（值变为6）再参与运算。因此10+6=16。

## 99. 以下关于Java数组的描述，错误的是哪一项？

- A. 数组是引用数据类型，创建数组时会在堆内存中分配空间
- B. 数组的长度是固定的，一旦创建无法修改
- C. 可以通过数组名.length获取数组的长度
- D. int[] arr = new int[5];语句中，arr[0]的默认值是1

**答案：** D

**解析：** Java中int类型数组的元素默认值是0，而非1。其他选项对数组的描述均正确。

## 100. 在Java中，以下关于String类的说法，正确的是哪一项？

- A. String对象是可变的，使用StringBuffer可以修改其内容
- B. String str = "abc"; str += "def";会创建一个新的String对象
- C. String类的equals()方法与==运算符的作用完全相同
- D. String类实现了Comparable接口，但没有重写compareTo()方法

**答案：** B

**解析：** String是不可变类，每次修改（如+=）都会创建新对象；StringBuffer是可变类，用于高效修改字符串。equals()比较内容，==比较地址；String重写了compareTo()方法用于字符串比较。

## 101. 以下代码中，关于方法重载的判断，正确的是哪一项？ public class OverloadTest {public void show(int a) {} public void show(String b) {} public int show(int a, int b){} public void show(double c) {} }

- A. 存在方法重载，因为方法名相同且参数列表不同
- B. 不存在方法重载，因为返回值类型不同
- C. 不存在方法重载，因为参数名称不同
- D. 不存在方法重载，因为方法修饰符相同

**答案：** A

**解析：** 方法重载的核心条件是**方法名相同，参数列表（参数个数、类型、顺序）不同**，与返回值类型、参数名称、修饰符无关。上述代码中四个show方法满足重载条件。

## 102. 在Java中，以下关于对象创建的描述，错误的是哪一项？

- A. 使用new关键字创建对象时，会调用类的构造方法
- B. 对象创建后，其成员变量会被初始化（默认值或显式赋值）
- C. 同一个类的多个对象共享类的静态成员变量
- D. 对象的引用变量存储在堆内存中，对象本身存储在栈内存中

**答案：** D

**解析：** Java中，**对象的引用变量存储在栈内存**，**对象本身（包括成员变量）存储在堆内存**。静态成员变量属于类，被所有对象共享。

## 103. 以下代码片段的运行结果是？ public class Test { public static void main(String[]args) { boolean flag = (10 >5) && (++10 >10); System.out.println(flag); } }

- A. true
- B. false
- C. 编译错误
- D. 运行时异常

**答案：** B

**解析：** 自增运算符（++）的操作数必须是**变量**，不能是常量（如10）。代码中++10违反语法规则，会导致编译错误。

## 104. 在Java中，以下关于protected访问修饰符的描述，正确的是哪一项？

- A. protected修饰的成员可以被不同包中的非子类访问
- B. protected修饰的成员只能被同一包中的类访问
- C. protected修饰的成员可以被同一包中的所有类访问，以及不同包中的子类访问
- D. protected修饰的成员只能被子类访问，无论是否在同一包中

**答案：** C

**解析：** protected的访问权限包括：同一包中的所有类，以及不同包中的子类。不同包中的非子类无法访问protected成员。

## 105. 以下代码中，关于构造方法的调用顺序，正确的是哪一项？ class Parent { Parent() {System.out.print("P"); } } class Child extends Parent { Child() { System.out.print("C"); } } public class Test { public static void main(String[] args) { new Child(); }}

- A. PC
- B. CP
- C. P
- D. C

**答案：** A

**解析：** 子类构造方法会默认调用父类的无参构造方法（若未显式调用super()）。因此创建Child对象时，先执行Parent的构造方法输出"P"，再执行Child的构造方法输出"C"，结果为"PC"。

## 106. 在Java中，以下关于数组初始化的描述，错误的是哪一项？

- A. int[] arr = {1,2,3};是静态初始化
- B. int[] arr = new int[3];是动态初始化，元素默认值为0
- C. int[][] arr = new int[2][3];是二维数组的动态初始化
- D. int[] arr = new int[]{1,2,3};是非法的初始化方式

**答案：** D

**解析：** int[] arr = new int[]{1,2,3};是合法的动态初始化方式，与静态初始化int[] arr={1,2,3}等价。

## 107. 以下代码片段的运行结果是？ public class Test { public static void main(String[]args) { String s1 = "hello"; String s2 = new String("hello"); System.out.println(s1 ==s2); } }

- A. true
- B. false
- C. 编译错误
- D. 运行时异常

**答案：** B

**解析：** s1指向字符串常量池中的"hello"对象，s2通过new关键字创建新的String对象（存储在堆内存）。==运算符比较对象地址，因此s1和s2地址不同，结果为false。

## 108. 在Java中，以下关于方法重载的说法，错误的是哪一项？

- A. 方法重载要求方法名必须相同
- B. 方法重载要求参数列表必须不同（个数、类型、顺序）
- C. 方法重载与方法的返回值类型无关
- D. 方法重载与方法的访问修饰符有关

**答案：** D

**解析：** 方法重载的判断标准仅与**方法名**和**参数列表**有关，与返回值类型、访问修饰符、异常抛出列表等无关。

## 109. 以下代码中，关于访问控制的判断，正确的是哪一项？ package com.example; public classParent { protected int num =10; } package com.test; import com.example.Parent; publicclass Child extends Parent { public void show() { System.out.println(num); } } publicclass Test { public static void main(String[] args) { Child c = new Child(); c.show();} }

- A. 编译错误，因为num是protected，Child无法访问
- B. 运行结果为10
- C. 运行时异常
- D. 编译错误，因为Parent和Child不在同一包中

**答案：** B

**解析：** protected修饰的成员可以被不同包中的子类访问。Child是Parent的子类（不同包），因此可以在Child的方法中访问Parent的protected成员num，运行结果为10。

## 110. 在Java中，以下关于构造方法的说法，错误的是哪一项？

- A. 构造方法可以被private修饰（如单例模式）
- B. 构造方法可以重载
- C. 子类构造方法必须显式调用父类的构造方法
- D. 如果父类没有无参构造方法，子类构造方法必须显式调用父类的有参构造方法

**答案：** C

**解析：** 子类构造方法**默认会隐式调用父类的无参构造方法**（若未显式调用super()），并非必须显式调用。只有当父类没有无参构造方法时，子类才必须显式调用父类的有参构造方法。

## 111. 以下代码片段的运行结果是？ public class Test { public static void main(String[]args) { int[] arr = {1,2,3,4,5}; for (int i =0; i < arr.length; i++) { if (arr[i] ==3){ break; } System.out.print(arr[i]); } } }

- A. 12
- B. 123
- C. 12345
- D. 无输出

**答案：** A

**解析：** 循环遍历数组，当arr[i]等于3时执行break跳出循环。因此只输出1和2。

## 112. 在Java中，以下关于StringBuffer和StringBuilder的描述，正确的是哪一项？

- A. StringBuffer是线程安全的，StringBuilder是线程不安全的
- B. StringBuffer是线程不安全的，StringBuilder是线程安全的
- C. 两者都是线程安全的
- D. 两者都是线程不安全的

**答案：** A

**解析：** StringBuffer的方法使用synchronized修饰，是线程安全的；StringBuilder的方法未加同步，线程不安全，但效率更高。

## 113. 以下代码中，关于对象创建的描述，正确的是哪一项？ class Person { private Stringname; public Person(String name) { this.name = name; } } public class Test { publicstatic void main(String[] args) { Person p = new Person("Alice"); } }

- A. 编译错误，因为Person类没有无参构造方法
- B. 编译错误，因为name是private成员
- C. 运行正常，创建了一个名为Alice的Person对象
- D. 运行时异常，因为构造方法参数不匹配

**答案：** C

**解析：** Person类显式定义了有参构造方法，因此编译器不会生成无参构造方法。创建对象时传入参数"Alice"，匹配有参构造方法，运行正常。private成员可以被类内部的构造方法访问。

## 114. 在Java中，以下关于运算符优先级的描述，正确的是哪一项？

- A. 算术运算符的优先级高于关系运算符
- B. 关系运算符的优先级高于算术运算符
- C. 逻辑运算符的优先级高于关系运算符
- D. 赋值运算符的优先级高于逻辑运算符

**答案：** A

**解析：** Java运算符优先级从高到低大致为：算术运算符（如+、*）>关系运算符（如>、==）>逻辑运算符（如&&、||）>赋值运算符（如=、+=）。

## 115. 以下代码片段的运行结果是？ public class Test { public static void main(String[]args) { String s = "Java"; s += "Programming"; System.out.println(s); } }

- A. Java
- B. Programming
- C. JavaProgramming
- D. 编译错误

**答案：** C

**解析：** String的+=操作会创建新的String对象，将原有字符串与新字符串拼接。因此s最终为"JavaProgramming"。

## 116. 下列关于Java访问控制修饰符的说法中，正确的有哪些？

- A. private修饰的成员只能在本类内部访问
- B. protected修饰的成员可以被同一包中的类以及不同包中的子类访问
- C. 默认访问权限（无修饰符）的成员可以被同一包中的类访问，不同包中的子类无法访问
- D. public修饰的成员只能被同一项目中的类访问

**答案：** ABC

**解析：** Java访问控制权限范围：private仅本类可见；protected对同包类和不同包子类可见；默认权限仅同包类可见；public对所有类可见（不受项目限制）。选项D错误。

## 117. 关于Java构造方法的描述，正确的是？

- A. 构造方法必须与类名相同
- B. 构造方法可以有返回值类型
- C. 一个类可以有多个构造方法（方法重载）
- D. 若类中无显式构造方法，JVM自动提供无参构造

**答案：** ACD

**解析：** 构造方法无返回值类型（含void），名称与类一致，支持重载。未显式定义时JVM提供无参构造。选项B错误。

## 118. 下列关于Java数组和字符串的操作，正确的是？

- A. 数组的length属性获取元素个数
- B. String类的length()方法获取字符数
- C. 数组创建后长度不可改变
- D. String对象不可变，修改会创建新对象

**答案：** ABCD

**解析：** 数组length是属性，String length是方法；数组长度固定；String是不可变类。所有选项正确。

## 119. 关于Java运算符和程序控制的说法，正确的有？

- A. 自增运算符优先级高于乘法
- B. 短路与（&&）左侧为false时右侧不执行
- C. Java7+允许switch使用String表达式
- D. for-each可遍历数组和Iterable集合

**答案：** BCD

**解析：** 乘法优先级高于自增；短路与特性；Java7+支持String switch；for-each适用于数组和Iterable。选项A错误。

## 120. 关于Java对象创建的说法，正确的是？

- A. new关键字调用构造方法创建对象
- B. 对象可调用本类所有方法（含private）
- C. 对象内存分配在堆区
- D. 未引用对象会被垃圾回收器回收

**答案：** ACD

**解析：** private方法仅本类内部可调用，对象无法直接访问；new创建对象在堆；垃圾回收处理未引用对象。选项B错误。

## 121. 关于Java方法重载的描述，正确的是？

- A. 方法名相同，参数列表不同（个数/类型/顺序）
- B. 构造方法也可重载
- C. 重载与返回值类型无关
- D. 静态方法和实例方法可构成重载

**答案：** ABCD

**解析：** 方法重载的核心是方法名相同且参数列表不同，与返回值、静态/实例属性无关，构造方法也支持重载。所有选项正确。

## 122. 下列关于Java基本数据类型的说法，正确的有？

- A. byte取值范围-128~127
- B. float精度低于double
- C. int相除结果为int（舍去小数）
- D. char可存储汉字（Unicode编码）

**答案：** ABCD

**解析：** byte范围正确；float是单精度，double双精度；整数除法截断小数；char用Unicode可存汉字。所有选项正确。

## 123. 类A和B同包，类C是A的子类且不同包。A的成员：private int a; int b; protected int c;public int d;正确的是？


**答案：** 

**解析：** 默认权限b仅同包可见；protected c对同包子类可见；private a仅本类可见；public d全可见。选项D错误。

## 124. 关于Java String类的操作，正确的是？

- A. "abc".equals("ABC")返回false
- B. "abc"+"def"创建新String对象
- C. StringBuffer和StringBuilder是可变字符串类
- D. intern()方法将字符串放入常量池（若不存在）

**答案：** ABCD

**解析：** equals区分大小写；字符串拼接创建新对象；StringBuffer/StringBuilder可变；intern()操作常量池。所有选项正确。

## 125. 关于Java循环结构的说法，正确的是？

- A. while循环条件可为任意布尔表达式
- B. do-while至少执行一次循环体
- C. for循环的初始化/条件/更新可省略
- D. 增强for循环可修改数组元素值

**答案：** ABC

**解析：** 增强for循环中的变量是副本，修改不影响原数组。选项D错误。

## 126. 当类有多个构造方法时，正确的是？

- A. this()调用本类其他构造方法
- B. this()必须在构造方法第一行
- C. super()调用父类构造方法
- D. super()和this()不能同时出现

**答案：** ABCD

**解析：** this()和super()都需在构造方法首行，故不能同时使用。所有选项正确。

## 127. 关于Java访问控制的错误说法是？

- A. public类可被所有类访问
- B. protected成员在不同包非子类中可访问
- C. 默认权限类仅同包可访问
- D. private静态成员可被同包类访问

**答案：** BD

**解析：** protected仅同包类和不同包子类可见；private静态成员仅本类可见。选项BD错误。

## 128. 关于Java类型转换的说法，正确的有？

- A. int到double是自动转换
- B. double到int需强制转换
- C. char可自动转换为int
- D. 布尔类型可转换为数值类型

**答案：** ABC

**解析：** 布尔类型与数值类型不可相互转换。选项D错误。

## 129. 关于Java类和方法的说法，正确的是？

- A. 一个类可实现多个接口
- B. 方法重载允许同名不同参数
- C. 静态方法可访问非静态成员
- D. 实例方法可访问静态成员

**答案：** ABD

**解析：** 静态方法无法访问非静态成员（无this引用）。选项C错误。

## 130. 关于Java数组的说法，正确的是？

- A. 数组可存储基本类型和引用类型
- B. 二维数组子数组长度可不同
- C. Arrays.sort()可排序数组
- D. 数组可用{}直接初始化

**答案：** ABCD

**解析：** 数组支持多种类型；二维数组是数组的数组，子数组长度可变；Arrays.sort()是常用排序方法；{}初始化是快捷方式。所有选项正确。

## 131. 关于Java构造方法访问控制的说法，正确的是？

- A. 构造方法可用public修饰
- B. 构造方法可用private修饰（如单例模式）
- C. 构造方法可用protected修饰
- D. 构造方法不能用默认访问权限

**答案：** ABC

**解析：** 构造方法支持所有访问修饰符，包括默认权限。选项D错误。

## 132. 关于Java字符串比较的说法，正确的是？

- A. ==比较String对象的引用地址
- B. equals()比较字符串内容
- C. "a" == "a"返回true（常量池）
- D. new String("a") == "a"返回true

**答案：** ABC

**解析：** new String创建的对象在堆，与常量池的"a"引用不同。选项D错误。

## 133. 关于Java分支结构的说法，正确的是？

- A. if-else可嵌套使用
- B. switch的case标签必须是常量表达式
- C. switch可无default分支
- D. break可跳出当前switch或循环

**答案：** ABCD

**解析：** 分支结构的基本特性：if-else嵌套、switch的case常量、default可选、break跳出结构。所有选项正确。

## 134. 关于Java对象的说法，正确的是？

- A. 对象是类的实例
- B. 每个对象有自己的成员变量副本
- C. 静态成员变量属于类，所有对象共享
- D. 可通过类名直接访问静态成员

**答案：** ABCD

**解析：** 对象是类的实例；实例变量每个对象独有；静态变量类共享；类名可直接访问静态成员。所有选项正确。

## 135. 关于Java构造方法重载的例子，正确的是？（类Test）

- A. public Test() {}与public Test(int a) {}构成重载
- B. public Test(String s) {}与public Test(int a,String s) {}构成重载
- C. public Test(int a) {}与public Test(int b) {}不构成重载
- D. public void Test() {}是构造方法

**答案：** ABC

**解析：** 构造方法无返回值类型（含void），选项D错误；重载需参数列表不同，选项C中参数类型相同故不构成重载。

## 136. Java中创建对象时，使用______关键字调用构造方法；若一个类没有显式定义构造方法，编译器会自动提供一个______构造方法。


**答案：** new；无参；

**解析：** new关键字用于实例化对象并调用构造方法；默认构造方法是编译器自动生成的无参数构造方法，当类中存在显式构造方法时会被覆盖。

## 137. 方法重载要求方法名______，参数列表______；方法的返回值类型______作为重载的判断依据。


**答案：** 相同；不同；不能；

**解析：** 方法重载的核心是方法名相同但参数列表（参数个数、类型或顺序）不同；返回值类型不影响重载判断，因为调用时无法通过返回值区分方法。

## 138. Java中的基本数据类型包括byte、short、int、long、float、double、boolean和______；其中，______类型用于表示单个字符。


**答案：** char；char；

**解析：** Java基本数据类型共8种，char类型占2字节，用于存储Unicode字符（如'A'、'中'）。

## 139. 数组的长度通过______属性获取，且数组一旦创建，长度______；二维数组的本质是______的数组。


**答案：** length；不可改变；数组元素为一维数组；

**解析：** 数组的length是属性而非方法；数组是定长容器，创建后长度固定；二维数组的每个元素都是一个一维数组，各子数组长度可不同。

## 140. 字符串常量池存储的是______的字符串对象；使用______方法可以比较字符串的内容是否相等，而==比较的是______。


**答案：** 字符串字面量；equals()；对象的引用地址；

**解析：** 字符串常量池用于缓存字符串字面量以节省内存；equals()方法重写后用于比较内容，==用于比较对象的内存地址。

## 141. 在switch语句中，JDK7及以上版本允许的表达式类型包括byte、short、int、char及其包装类，以及______和______。


**答案：** String；枚举类型(enum)；

**解析：** JDK7扩展了switch的表达式类型，支持String和enum；JDK14进一步支持switch表达式（箭头语法）。

## 142. Java中的三元运算符格式为______ ? ______ : ______；其结果类型由______决定。


**答案：** 条件表达式；表达式1；表达式2；两个表达式的共同父类型；

**解析：** 三元运算符是条件判断的简写形式，条件为真时执行表达式1，否则执行表达式2；结果类型需兼容两个表达式的类型（如int和double的结果为double）。

## 143. 类的访问修饰符有public和______（默认）；若一个类使用public修饰，则其类名必须与______一致。


**答案：** 包访问权限；文件名；

**解析：** 类的访问修饰符仅支持public和默认（无修饰符）；public类必须放在同名的.java文件中，否则编译报错。

## 144. 构造方法的名称必须与______相同，且______返回值类型（包括void）；若要调用本类的其他构造方法，需使用______关键字。


**答案：** 类名；没有；this；

**解析：** 构造方法是特殊的方法，用于初始化对象；this()用于在构造方法中调用本类的其他构造方法，且必须放在第一行。

## 145. 数组初始化时，动态初始化只指定______，由系统分配默认值；静态初始化通过______直接指定元素值，此时______可以省略。


**答案：** 数组长度；大括号{}；new类型[]；

**解析：** 动态初始化如int[] arr = new int[5]；静态初始化如int[] arr = {1,2,3}（可省略new int[]），但作为方法参数时需写全。

## 146. String类是______类，其对象一旦创建就______被修改；若需频繁修改字符串，应使用______类。


**答案：** 不可变(immutable)；不能；StringBuilder或StringBuffer；

**解析：** String的不可变性保证了线程安全和字符串常量池的复用；StringBuilder是非线程安全但高效，StringBuffer是线程安全但低效。

## 147. Java中的短路与运算符是______，短路或运算符是______；它们与逻辑与(&)、逻辑或(|)的区别是______。


**答案：** &&；||；短路运算符会提前终止运算（当结果已确定时）；

**解析：** &&当第一个条件为false时，第二个条件不执行；||当第一个条件为true时，第二个条件不执行；&和|则会执行所有条件。

## 148. 在for-each循环中，______直接遍历数组或集合的元素，而无需使用______；但该循环______修改数组或集合的元素值（基本类型）。


**答案：** 可以；索引；不能；

**解析：** for-each循环语法为for(元素类型变量 :数组/集合)，适用于遍历但不适用于修改基本类型元素（因为是值传递）。

## 149. 类的成员方法中，若要访问本类的静态成员，需使用______关键字或______；静态方法______直接访问非静态成员。


**答案：** 类名；静态成员名(直接访问)；不能；

**解析：** 静态成员属于类而非对象，可通过类名或直接访问；静态方法中没有this指针，因此无法访问非静态成员（属于对象）。

## 150. 默认访问权限的类成员，能被______和______访问；若子类与父类不同包，则______访问父类的默认成员。


**答案：** 本类；同包类；不能；

**解析：** 默认访问权限（包访问权限）的作用域仅限于当前包内的类，不同包子类无法访问。

## 151. 方法重载时，参数列表的不同包括______、______和______；方法的访问修饰符和异常声明______作为重载的判断依据。


**答案：** 参数个数不同；参数类型不同；参数顺序不同；不能；

**解析：** 方法重载的唯一判断标准是参数列表，与访问修饰符、返回值、异常等无关。

## 152. 创建对象的过程包括______、______和______；其中，______阶段为对象分配内存并初始化默认值。


**答案：** 类加载(若未加载)；内存分配；初始化(构造方法调用)；内存分配；

**解析：** 对象创建的核心步骤：类加载→分配内存（默认初始化）→显式初始化（构造方法）。

## 153. Java中的自动装箱是指将______类型转换为对应的______类型；自动拆箱则是相反过程，例如将______转换为int。


**答案：** 基本数据；包装；Integer；

**解析：** 自动装箱/拆箱是JDK5引入的特性，方便基本类型与包装类的转换，如int→Integer（装箱）、Integer→int（拆箱）。

## 154. 在异常处理中，______块用于捕获异常，______块用于释放资源（无论是否发生异常）；若try块中没有异常，______块不会执行。


**答案：** catch；finally；catch；

**解析：** try-catch-finally结构中，catch捕获指定异常，finally确保资源释放；若try无异常，直接执行finally，跳过catch。

## 155. 在Java中，当子类重写父类的protected方法时，子类方法的访问权限可以修改为private。


**答案：** 错误

**解析：** Java中方法重写的访问权限遵循“子类权限不能比父类更严格”的规则：父类方法为protected时，子类重写方法的权限只能是protected或public，若改为private则会编译报错。

## 156. Java的构造方法可以被static关键字修饰。


**答案：** 错误

**解析：** 构造方法用于创建对象实例，static修饰的成员属于类而非实例，因此构造方法不能被static修饰，否则会导致编译错误。

## 157. int类型变量与long类型变量进行运算时，结果的类型会自动提升为long。


**答案：** 正确

**解析：** Java中不同基本数据类型运算时遵循“小范围类型向大范围类型自动提升”的规则：int（4字节）小于long（8字节），运算结果会自动提升为long类型。

## 158. 在try-catch-finally结构中，若try块中存在return语句，finally块的代码不会执行。


**答案：** 错误

**解析：** 无论try块中是否有return、异常是否发生，finally块的代码都会执行（除非在try/catch中调用System.exit(0)终止JVM）。

## 159. Java数组的长度在创建后可以通过数组对象的length属性修改。


**答案：** 错误

**解析：** Java数组是定长结构，一旦通过new关键字初始化（如int[] arr = new int[5]），其长度就固定不变，length属性是只读的，无法修改。

## 160. String类的concat()方法会修改原字符串的内容。


**答案：** 错误

**解析：** Java的String是不可变类（immutable），concat()方法会创建一个新的字符串对象来存储拼接后的结果，原字符串对象不会被修改。

## 161. 在Java中，接口中的所有方法默认是public abstract的，因此可以省略这些修饰符。


**答案：** 正确

**解析：** Java接口的方法默认具有public abstract修饰（JDK8及以后允许static和default方法，但普通方法仍遵循此规则），显式书写或省略效果一致。

## 162. 对象的默认访问权限（包访问权限）意味着该对象可以被所有包中的类访问。


**答案：** 错误

**解析：** 默认访问权限（无修饰符）的成员仅能被同一个包内的类访问，不同包的类无法访问，其权限范围小于protected和public。

## 163. 方法重载要求方法的参数列表（参数个数、类型或顺序）必须不同，返回值类型可以相同也可以不同。


**答案：** 正确

**解析：** 方法重载的核心判定依据是“参数列表不同”，返回值类型不影响重载判定，只要参数列表有差异即可构成重载。

## 164. 使用new关键字创建对象时，构造方法会先执行父类的构造方法，再执行子类的构造方法。


**答案：** 正确

**解析：** Java对象实例化时遵循“先父类后子类”的构造顺序：子类构造方法第一行默认调用super()（父类无参构造），因此父类构造先执行。

## 165. private修饰的成员变量可以被同一个类中的静态方法直接访问。


**答案：** 正确

**解析：** private成员的访问权限是“同一个类内”，静态方法属于类本身，因此在同一个类中，静态方法可以直接访问private成员变量（无需对象实例）。

## 166. for-each循环（增强for循环）可以用于遍历Java中的所有数组和集合，包括不可变集合。


**答案：** 错误

**解析：** for-each循环依赖于Iterable接口，Java数组虽然可以用for-each遍历，但数组本身并未实现Iterable接口；此外，不可变集合若实现了Iterable则可以遍历，但该说法的“所有”表述不准确（如原生数组不属Iterable）。

## 167. Java中，当两个对象的equals()方法返回true时，它们的hashCode()方法必须返回相同的值。


**答案：** 正确

**解析：** 根据Java的规范，equals()和hashCode()需满足一致性：若a.equals(b)为true，则a.hashCode()必须等于b.hashCode()（反之不强制）。

## 168. 构造方法可以有返回值类型，只要返回值类型与类名一致。


**答案：** 错误

**解析：** 构造方法的定义规则是“无返回值类型”（连void都不能写），若添加返回值类型，则该方法不再是构造方法，而是普通方法。

## 169. 在Java中，数组是对象，因此可以调用Object类的所有方法（如toString()、equals()等）。


**答案：** 正确

**解析：** Java中数组是引用类型，属于Object的子类，因此数组对象可以调用Object类的所有非private方法。

## 170. protected修饰的成员变量可以被不同包中的子类直接访问，无需通过方法调用。


**答案：** 正确

**解析：** protected权限的成员允许“同一个包内的类”或“不同包的子类”访问，不同包的子类可以直接访问父类的protected成员（需通过子类实例或super关键字）。

## 171. 方法重载时，若参数列表中的参数类型为父类和子类，调用方法时会优先匹配子类类型的参数。


**答案：** 正确

**解析：** Java方法调用的参数匹配遵循“精确匹配优先”原则：若存在子类类型的参数方法，会优先选择该方法，而非父类类型的重载方法。

## 172. StringBuilder的append()方法会修改原对象的内容，因此是可变的。


**答案：** 正确

**解析：** StringBuilder是可变字符串类，append()等方法直接修改对象内部的字符数组，不会创建新对象，与String的不可变性相反。

## 173. 在Java中，若一个类没有显式定义构造方法，编译器会自动生成一个默认的无参构造方法。


**答案：** 正确

**解析：** 当类未定义任何构造方法时，Java编译器会隐式添加一个无参、访问权限与类一致的默认构造方法。

## 174. public修饰的类必须与包含该类的文件名相同（包括大小写）。


**答案：** 正确

**解析：** Java的命名规范要求：若类被public修饰，则类名必须与.java文件名完全一致（大小写敏感），否则会编译错误。

## 175. 在Java中，当子类继承父类时，子类构造方法中若未显式调用父类构造方法，JVM会自动插入对父类无参构造方法的调用。请分析：若父类仅定义了带参构造方法而未定义无参构造方法，子类构造方法未显式调用父类带参构造方法时，会出现什么编译错误？并说明如何修改以解决该问题。


**答案：** 会出现“Implicit super constructor XXX() is undefined. Must explicitly invokeanother constructor”的编译错误。解决方法有两种：1. 在父类中显式定义无参构造方法；2. 在子类构造方法的第一行显式调用父类的带参构造方法（使用super(参数列表)）。

**解析：** 子类构造方法默认会调用父类无参构造，若父类无无参构造且子类未显式调用父类带参构造，则JVM无法完成父类初始化，导致编译错误。通过显式定义父类无参构造或子类显式调用父类带参构造，可满足父类初始化要求。

## 176. Java中的方法重载需要满足哪些条件？请结合方法的参数列表、返回值类型、访问修饰符分析，并举例说明返回值类型为何不参与方法重载的判定。


**答案：** 方法重载的条件：1.方法名相同；2. 参数列表（参数个数、类型、顺序）不同。返回值类型和访问修饰符不影响重载判定。例如：定义method(int a)和method(String b)构成重载；但method(int a)和int method(int a)不构成重载，因为JVM无法通过返回值类型区分调用的方法（如int x= method(1)和method(1)的调用形式可能冲突）。

**解析：** 方法重载是编译时多态的体现，JVM在编译阶段根据方法签名（方法名+参数列表）确定调用的方法。返回值类型不参与签名构成，因此无法作为重载判定依据；访问修饰符仅影响方法可见性，与方法签名无关。

## 177. 请解释Java中数组的“协变”特性，并分析该特性可能导致的运行时异常。同时，说明如何通过泛型集合（如ArrayList）避免此类异常。


**答案：** Java数组是协变的：若类A是类B的子类，则A[]是B[]的子类。例如：Object[] arr = newString[5]; arr[0] = new Integer(1);会抛出ArrayStoreException运行时异常，因为实际数组类型是String[]，无法存储Integer对象。使用泛型集合（如ArrayList ）时，编译阶段会检查元素类型，若尝试添加非String类型元素会直接编译错误，避免运行时异常。

**解析：** 数组协变允许向上转型，但实际数组的运行时类型仍为原类型，添加不兼容类型元素会触发运行时异常。泛型集合通过编译时类型擦除前的类型检查，提前阻止不兼容元素的添加，保证类型安全。

## 178. Java中访问控制修饰符有public、protected、默认（包访问）、private，请详细对比这四种修饰符在不同作用域（同一类、同一包、不同包子类、不同包非子类）下的可见性，并举例说明protected修饰符的特殊可见性规则。


**答案：** 可见性对比：1. private：仅同一类可见；2. 默认：同一类、同一包可见；3. protected：同一类、同一包、不同包子类可见；4. public：所有作用域可见。protected的特殊规则：子类在不同包中时，只能通过子类对象访问父类的protected成员，不能通过父类对象直接访问。例如：package A; class Parent { protected int x; } package B; class Child extends Parent {void test() { Child c = new Child(); c.x =1; //合法 Parent p = new Parent(); p.x =1; //非法 } }

**解析：** 访问控制修饰符用于封装，控制类成员的可见范围。protected修饰符的设计目的是允许子类继承父类的成员，同时限制非子类的访问，其跨包子类的访问需通过子类对象实现。

## 179. 请分析Java中字符串常量池的工作机制，并解释以下代码的输出结果及原因：String s1 ="abc"; String s2 = new String("abc"); String s3 = s2.intern(); System.out.println(s1 ==s2); System.out.println(s1 == s3);


**答案：** 输出结果：false、true。原因：字符串常量池存储编译期确定的字符串常量。s1直接指向常量池中的"abc"；s2通过new创建，指向堆中的新对象；s2.intern()会将堆中的"abc"加入常量池（若不存在），并返回常量池中的引用，因此s3与s1指向同一常量池对象。

**解析：** 字符串常量池通过复用相同字符串对象节省内存。new String()会在堆中创建新对象，intern()方法可将堆中字符串对象的引用加入常量池（若常量池中无该字符串），从而实现常量池复用。

## 180. Java中构造方法的作用是什么？请对比构造方法与普通方法的区别（从方法名、返回值、调用方式、修饰符等方面分析），并说明构造方法重载的意义。


**答案：** 构造方法的作用是初始化对象，为对象的成员变量赋初始值。与普通方法的区别：1.方法名：构造方法与类名相同，普通方法任意；2. 返回值：构造方法无返回值（连void都没有），普通方法有返回值或void；3.调用方式：构造方法通过new关键字调用，普通方法通过对象或类（静态方法）调用；4.修饰符：构造方法可使用访问修饰符，普通方法还可使用static、final等修饰符。构造方法重载的意义：允许对象以不同的初始状态创建，满足多样化的对象初始化需求。

**解析：** 构造方法是对象创建过程中必须调用的特殊方法，负责对象的初始化。重载的构造方法提供了灵活的对象创建方式，适应不同的参数输入场景。

## 181. 请解释Java中“自动装箱”和“自动拆箱”的概念，并分析以下代码可能导致的问题：Integer a =100; Integer b =100; Integer c =200; Integer d =200; System.out.println(a ==b); System.out.println(c == d);


**答案：** 自动装箱是将基本数据类型自动转换为对应的包装类对象；自动拆箱是将包装类对象自动转换为基本数据类型。代码输出：true、false。原因：Integer类对-128到127之间的整数进行了缓存，a和b指向缓存中的同一对象；c和d超过缓存范围，会创建新对象，因此==比较（地址比较）结果为false。

**解析：** 自动装箱/拆箱简化了基本类型与包装类的转换，但需注意缓存机制的影响。对于包装类对象的比较，应使用equals()方法而非==，避免因缓存范围导致的逻辑错误。

## 182. Java中程序控制结构包括顺序、分支、循环，请设计一个程序，使用嵌套循环和条件判断实现以下功能：打印一个10行的等腰三角形，其中奇数行打印星号（*），偶数行打印井号（#），每行的字符数等于行号（如第1行1个*，第2行2个#，第3行3个*，以此类推）。要求代码中包含至少两种循环结构（for、while、do-while选两种）。


**答案：** 参考代码： public class Triangle { public static void main(String[] args) { introws =10; for (int i =1; i <= rows; i++) { //外层for循环控制行数 int j =1; while (j <=i) { //内层while循环控制每行字符数 if (i %2 ==1) { System.out.print("*"); } else {System.out.print("#"); } j++; } System.out.println(); } } }

**解析：** 该程序通过外层for循环遍历行数，内层while循环控制每行字符的输出数量，结合条件判断区分奇偶行的字符类型。嵌套循环和条件判断的结合实现了复杂的输出逻辑，体现了程序控制结构的应用。

## 183. 请分析Java中方法参数传递的机制，并解释以下代码的输出结果及原因：public classParamTest { public static void changeInt(int x) { x =10; } public static voidchangeArray(int[] arr) { arr[0] =10; } public static void changeString(String s) { s ="new"; } public static void main(String[] args) { int a =5; changeInt(a); System.out.println(a); int[] arr = {1,2,3}; changeArray(arr); System.out.println(arr[0]); String s= "old"; changeString(s); System.out.println(s); } }


**答案：** 输出结果：5、10、old。原因：Java中方法参数传递是值传递。对于基本数据类型（如int），传递的是值的副本，修改副本不影响原变量；对于引用数据类型（如数组、String），传递的是引用的副本：数组的引用副本指向原数组对象，修改数组元素会影响原数组；String是不可变对象，修改引用副本指向新对象（"new"），原引用仍指向"old"，因此原变量不变。

**解析：** 值传递是Java参数传递的核心机制，需区分基本类型和引用类型的传递差异。引用类型传递的是引用的副本，若修改的是对象内部状态（如数组元素）会影响原对象，若修改的是引用本身（如String重新赋值）则不影响原引用。

## 184. 请解释Java中“静态方法不能访问非静态成员”的原因，并分析以下代码的错误之处及修改方法：public class StaticTest { private int num =5; public static void printNum() {System.out.println(num); } public static void main(String[] args) { printNum(); } }


**答案：** 原因：静态方法属于类，在类加载时初始化，此时非静态成员（属于对象）尚未创建，因此静态方法无法访问非静态成员。代码错误：printNum()是静态方法，直接访问非静态成员num。修改方法：1. 将num改为静态成员（static int num =5）；2. 在printNum()中创建StaticTest对象，通过对象访问num（如StaticTest obj = new StaticTest(); System.out.println(obj.num);）。

**解析：** 静态成员与类关联，非静态成员与对象关联。静态方法的生命周期早于对象，因此无法直接访问依赖对象存在的非静态成员。通过静态化成员或创建对象的方式，可解决静态方法访问非静态成员的问题。

## 185. Java中字符串的不可变性是指什么？请从字符串的内部实现（如char数组）分析不可变性的原因，并说明不可变性带来的优缺点。


**答案：** 字符串不可变性是指字符串对象创建后，其内容（字符序列）无法被修改。内部实现：String类使用private final char[] value存储字符，final修饰符保证value数组引用不可变，private修饰符防止外部直接访问修改数组元素。优点：1.线程安全；2.可缓存哈希值，提高哈希表效率；3.字符串常量池复用，节省内存。缺点：修改字符串时会创建新对象，增加内存开销（如频繁拼接字符串建议使用StringBuilder）。

**解析：** 不可变性是String类的核心特性，通过final和private修饰符实现。不可变性带来了安全性和性能优势，但在频繁修改场景下会导致内存浪费，需结合StringBuilder等可变字符串类使用。

## 186. 请设计一个Java类，包含以下功能：1.私有成员变量name（String）和age（int）；2.带参构造方法初始化name和age；3.重载构造方法，仅初始化name（age默认值为18）；4. public方法getInfo()返回包含name和age的字符串；5. protected方法setAge(int age)修改age（需判断age>0）；6.测试类中创建两个对象，分别使用两个构造方法初始化，并调用getInfo()和setAge()方法。要求代码符合封装和访问控制的原则。


**答案：** 参考代码： //主类public class Person { private String name; private int age; //带参构造方法 public Person(String name, int age) { this.name = name; if (age >0) this.age= age; else this.age =18; } //重载构造方法 public Person(String name) { this(name,18);} public String getInfo() { return "Name: " + name + ", Age: " + age; } protected voidsetAge(int age) { if (age >0) this.age = age; } } //测试类public class PersonTest {public static void main(String[] args) { Person p1 = new Person("Alice",20); System.out.println(p1.getInfo()); p1.setAge(25); System.out.println(p1.getInfo()); Person p2 = newPerson("Bob"); System.out.println(p2.getInfo()); } }

**解析：** 该类通过私有成员变量实现封装，构造方法重载满足不同初始化需求，访问修饰符控制成员的可见性。setAge()方法的参数校验保证了数据的合法性，体现了面向对象的封装和安全性原则。

## 187. 请解释Java中“多态”的概念，并结合方法重写和向上转型分析以下代码的输出结果及原因：class Animal { public void makeSound() { System.out.println("Animal sound"); } } classDog extends Animal { @Override public void makeSound() { System.out.println("Woof"); }} class Cat extends Animal { @Override public void makeSound() { System.out.println("Meow"); } } public class PolymorphismTest { public static void main(String[] args) {Animal a1 = new Dog(); Animal a2 = new Cat(); a1.makeSound(); a2.makeSound(); } }


**答案：** 输出结果：Woof、Meow。多态是指同一方法在不同对象上有不同的实现，通过方法重写和向上转型实现。向上转型：Animal a1 = new Dog()将Dog对象赋值给Animal类型引用；方法重写：Dog和Cat类重写了Animal类的makeSound()方法。运行时，JVM根据对象的实际类型（Dog、Cat）调用对应的重写方法，体现了动态多态性。

**解析：** 多态是面向对象的核心特性之一，通过向上转型实现类型统一，通过方法重写实现行为多样化。运行时多态性由JVM的动态方法分派机制实现，根据对象的实际类型调用相应的方法。

## 188. Java中数组的初始化方式有哪几种？请分别举例说明静态初始化和动态初始化的区别，并分析以下代码的错误之处：int[] arr1 = new int[5]{1,2,3,4,5}; int[] arr2 = {1,2,3}; arr2 =new int[10]; arr2[0] =10; System.out.println(arr2.length);


**答案：** 数组初始化方式：1.静态初始化：直接指定元素值（如int[] arr = {1,2,3}或int[] arr =new int[]{1,2,3}）；2.动态初始化：指定数组长度，元素默认初始化（如int[] arr = new int[5]）。静态初始化与动态初始化的区别：静态初始化需显式指定元素值，长度由元素个数决定；动态初始化需指定长度，元素值为默认值（int为0，对象为null）。代码错误：int[] arr1 = new int[5]{1,2,3,4,5}是非法的，静态初始化使用new int[]{}时不能指定长度。修改为int[] arr1 = newint[]{1,2,3,4,5}。arr2的输出结果为10，因为arr2被重新赋值为长度为10的新数组。

**解析：** 数组初始化的语法需严格遵守，静态初始化不能同时指定长度和元素值。数组引用是可变的，可重新指向新的数组对象，此时数组长度为新数组的长度。

## 189. 请分析Java中运算符的优先级，解释以下表达式的计算结果及步骤：int a =3; int b =4;int c = a++ * --b + (a > b ? a : b);


**答案：** 计算步骤：1.运算符优先级：--（前缀自减）> *（乘法）> ?:（三目运算符）> ++（后缀自增）。2.计算--b：b=4变为3；3.计算a++ * --b：a当前为3，乘法结果为3*3=9，之后a自增为4；4.计算三目运算符：a=4 > b=3为true，结果为4；5.最终c=9+4=13。结果：13。

**解析：** 运算符优先级决定了表达式的计算顺序，前缀自减的优先级高于乘法，后缀自增在当前表达式计算后执行。三目运算符的优先级低于乘法，需先计算乘法部分再进行三目判断。

## 190. Java中“对象创建”的过程包括哪些步骤？请从类加载、内存分配、成员变量初始化、构造方法调用等方面详细说明，并分析以下代码中对象创建的顺序：class A { A() { System.out.println("A constructor"); } } class B extends A { B() { System.out.println("B constructor"); }} public class ObjectCreation { public static void main(String[] args) { B b = new B();} }


**答案：** 对象创建步骤：1.类加载：若类未加载，JVM加载类的字节码到方法区；2.内存分配：在堆中为对象分配内存空间；3.成员变量初始化：先初始化父类成员变量，再初始化子类成员变量（默认值或显式初始化）；4.构造方法调用：先调用父类构造方法（默认或显式），再调用子类构造方法。代码输出顺序：A constructor、B constructor。原因：子类构造方法默认调用父类无参构造，因此先执行父类构造，再执行子类构造。

**解析：** 对象创建是一个分层初始化的过程，父类的初始化优先于子类，保证了父类成员的正确初始化。构造方法的调用顺序遵循继承层次，体现了面向对象的继承特性。

## 191. 请设计一个Java程序，实现以下功能：输入一个字符串，统计其中每个字符出现的次数（区分大小写），并按字符顺序输出结果。要求使用数组（char[]）和循环结构实现，禁止使用集合类（如HashMap）。


**答案：** 参考代码： import java.util.Scanner; public class CharCount { public static voidmain(String[] args) { Scanner scanner = new Scanner(System.in); System.out.print("请输入字符串："); String str = scanner.nextLine(); char[] chars = str.toCharArray(); int[]count = new int[256]; // ASCII码范围0-255 for (char c : chars) { count[(int)c]++; }System.out.println("字符出现次数："); for (int i =0; i <256; i++) { if (count[i] >0) {System.out.println((char)i + ": " + count[i]); } } scanner.close(); } }

**解析：** 该程序将字符串转换为char数组，利用ASCII码作为数组索引统计字符出现次数。通过循环遍历字符数组和ASCII码范围，实现了字符计数功能，体现了数组的应用和循环结构的使用。

## 192. Java中“封装”的概念是什么？请结合访问控制修饰符和getter/setter方法分析封装的实现方式，并说明封装的优点。


**答案：** 封装是将类的成员变量私有化，通过公共的getter和setter方法控制对成员变量的访问，隐藏对象的内部实现细节。实现方式：1. 使用private修饰成员变量，禁止外部直接访问；2.提供public的getter方法获取成员变量值；3.提供public的setter方法修改成员变量值（可添加参数校验）。优点：1.隐藏实现细节，提高代码安全性；2.便于修改内部实现，不影响外部代码；3.控制成员变量的访问权限，保证数据合法性。

**解析：** 封装是面向对象的三大特性之一，通过访问控制和方法封装实现数据的保护和代码的模块化。getter/setter方法为外部提供了安全的访问接口，同时保留了内部实现的灵活性。

## 193. 请解释Java中“静态块”和“实例块”的概念，并分析以下代码的执行顺序：classBlockTest { static { System.out.println("静态块1"); } { System.out.println("实例块1");} BlockTest() { System.out.println("构造方法"); } static { System.out.println("静态块2"); } { System.out.println("实例块2"); } public static void main(String[] args) {BlockTest b1 = new BlockTest(); BlockTest b2 = new BlockTest(); } }


**答案：** 静态块是类加载时执行的代码块，用于初始化静态成员；实例块是对象创建时执行的代码块，用于初始化实例成员（在构造方法之前执行）。执行顺序：1.类加载时执行所有静态块（按定义顺序）：静态块1、静态块2；2. 创建b1时，执行实例块（按定义顺序）：实例块1、实例块2，再执行构造方法；3. 创建b2时，重复执行实例块和构造方法。输出顺序：静态块1、静态块2、实例块1、实例块2、构造方法、实例块1、实例块2、构造方法。

**解析：** 静态块的执行时机是类加载，仅执行一次；实例块的执行时机是对象创建，每次创建对象都会执行。静态块和实例块的执行顺序遵循定义顺序，实例块在构造方法之前执行。

## 194. Java中“异常处理”的机制是什么？请结合try-catch-finally语句和throw/throws关键字分析异常处理的流程，并设计一个程序，模拟文件读取时可能出现的FileNotFoundException，使用异常处理机制捕获并处理该异常。


**答案：** 异常处理机制是通过try块包裹可能抛出异常的代码，catch块捕获并处理异常，finally块执行无论是否发生异常都需执行的代码；throw用于手动抛出异常，throws用于声明方法可能抛出的异常。参考代码： import java.io.File; import java.io.FileReader; import java.io.FileNotFoundException; public class ExceptionTest { public static void readFile(Stringpath) throws FileNotFoundException { File file = new File(path); if (!file.exists()) {throw new FileNotFoundException("文件不存在：" + path); } FileReader fr = new FileReader(file); //读取文件操作（省略） } public static void main(String[] args) { try { readFile("test.txt"); System.out.println("文件读取成功"); } catch (FileNotFoundException e) {System.out.println("异常处理：" + e.getMessage()); } finally { System.out.println("无论是否异常，都会执行"); } } }

**解析：** 该程序通过throw手动抛出FileNotFoundException，方法声明throws告知调用者可能的异常；main方法中try块调用readFile()，catch块捕获并处理异常，finally块执行收尾操作。异常处理机制保证了程序在出现异常时的优雅处理，避免程序崩溃。
`;

// ===== Application Logic =====
(function() {
'use strict';

// ===== Type Constants =====
var TYPE_LABELS = {
    single: '单选题', multiple: '多选题', tf: '判断题',
    fill: '填空题', short: '简答题', analysis: '分析题'
};
var BADGE_CLASS = {
    single: '', multiple: 'type-multiple', tf: 'type-tf',
    fill: 'type-fill', short: 'type-short', analysis: 'type-analysis'
};
var SCORABLE = ['single', 'multiple', 'tf'];

// ===== State =====
var quizData = [];
var state = {
    currentIndex: 0,
    answers: {},
    revealed: [],
    filter: 'all',
    submitted: false
};

// ===== DOM Cache =====
var dom = {};

function initDom() {
    dom.badge = document.getElementById('question-badge');
    dom.number = document.getElementById('question-number');
    dom.text = document.getElementById('question-text');
    dom.options = document.getElementById('question-options');
    dom.showBtn = document.getElementById('show-answer-btn');
    dom.ansContent = document.getElementById('answer-content');
    dom.ansText = document.getElementById('correct-answer-text');
    dom.explArea = document.getElementById('explanation-area');
    dom.explText = document.getElementById('explanation-text');
    dom.prev = document.getElementById('prev-btn');
    dom.next = document.getElementById('next-btn');
    dom.progress = document.getElementById('nav-progress');
    dom.submitBtn = document.getElementById('submit-btn');
    dom.grid = document.getElementById('question-grid');
    dom.filters = document.getElementById('filter-tabs');
    dom.statAns = document.getElementById('stat-answered');
    dom.statTotal = document.getElementById('stat-total');
    dom.statOk = document.getElementById('stat-correct');
    dom.statWrong = document.getElementById('stat-incorrect');
    dom.modal = document.getElementById('result-modal');
    dom.summary = document.getElementById('result-summary');
    dom.review = document.getElementById('review-btn');
    dom.restart = document.getElementById('restart-btn');
    dom.sidebar = document.getElementById('sidebar');
    dom.sidebarToggle = document.getElementById('sidebar-toggle');
    dom.sidebarBackdrop = document.getElementById('sidebar-backdrop');
    dom.sidebarAnswered = document.getElementById('sidebar-answered');
    dom.sidebarCorrect = document.getElementById('sidebar-correct');
    dom.sidebarIncorrect = document.getElementById('sidebar-incorrect');
}

// ===== Utilities =====
function esc(s) {
    var d = document.createElement('div');
    d.textContent = s;
    return d.innerHTML;
}

function formatText(t) {
    if (!t) return '';
    return esc(t).replace(/`([^`]+)`/g, '<code>$1</code>').replace(/\n/g, '<br>');
}

// ===== Parser =====
function parseQuestions(md) {
    var qs = [];
    var blocks = md.split(/\n(?=## \d+[.、])/);
    for (var i = 0; i < blocks.length; i++) {
        var b = blocks[i].trim();
        if (!b) continue;
        var hm = b.match(/^## (\d+)[.、]\s*([\s\S]*)/);
        if (!hm) continue;
        var num = parseInt(hm[1]);
        var content = hm[2];

        // Options
        var opts = [];
        var orx = /^- ([A-Z])[.、]\s*(.*)/gm;
        var om;
        while ((om = orx.exec(content)) !== null) {
            opts.push({ label: om[1], text: om[2].trim() });
        }

        // Answer
        var am = content.match(/\*\*答案[：:]\*\*\s*([\s\S]*?)(?=\n\*\*解析|$)/);
        var ans = am ? am[1].trim() : '';

        // Explanation
        var em = content.match(/\*\*解析[：:]\*\*\s*([\s\S]*)/);
        var expl = em ? em[1].trim() : '';

        // Question text
        var qt = content;
        var cutOpt = qt.search(/\n- [A-Z]/);
        var cutAns = qt.search(/\*\*答案/);
        var cut = qt.length;
        if (cutOpt > -1) cut = Math.min(cut, cutOpt);
        if (cutAns > -1) cut = Math.min(cut, cutAns);
        qt = qt.substring(0, cut).trim();

        // Type detection
        var type;
        if (opts.length > 0) {
            type = (/^[A-Z]{2,}$/.test(ans)) ? 'multiple' : 'single';
        } else if (/^(正确|错误)$/.test(ans)) {
            type = 'tf';
        } else if (/_{3,}/.test(qt)) {
            type = 'fill';
        } else if (ans.length > 200) {
            type = 'analysis';
        } else {
            type = 'short';
        }

        qs.push({
            id: num, type: type, text: qt,
            options: opts.length > 0 ? opts : null,
            answer: ans, explanation: expl
        });
    }
    return qs;
}

// ===== Helpers =====
function getFiltered() {
    if (state.filter === 'all') return quizData;
    return quizData.filter(function(q) { return q.type === state.filter; });
}

function isScorable(q) {
    return SCORABLE.indexOf(q.type) !== -1;
}

function isRevealed(q) {
    return state.revealed.indexOf(q.id) !== -1;
}

function checkCorrect(q) {
    var ua = state.answers[q.id];
    if (ua === undefined || ua === '') return false;
    if (q.type === 'multiple') {
        return ua.split('').sort().join('') === q.answer.split('').sort().join('');
    }
    return ua === q.answer;
}

// ===== Grid =====
function renderGrid() {
    var html = '';
    quizData.forEach(function(q) {
        var cls = 'grid-btn';
        if (state.filter !== 'all' && q.type !== state.filter) cls += ' hidden-by-filter';
        if (isRevealed(q)) {
            if (isScorable(q)) {
                cls += checkCorrect(q) ? ' correct' : ' incorrect';
            } else {
                cls += ' revealed';
            }
        }
        html += '<button class="' + cls + '" data-id="' + q.id + '">' + q.id + '</button>';
    });
    dom.grid.innerHTML = html;
    updateGridActive();
}

function updateGridActive() {
    var filtered = getFiltered();
    var cur = filtered[state.currentIndex];
    if (!cur) return;
    dom.grid.querySelectorAll('.grid-btn').forEach(function(btn) {
        btn.classList.toggle('active', parseInt(btn.dataset.id) === cur.id);
    });
}

// ===== Render Question =====
function renderQuestion() {
    var filtered = getFiltered();
    if (filtered.length === 0) {
        dom.text.innerHTML = '<div class="loading-screen"><p>该分类下暂无题目</p></div>';
        dom.options.innerHTML = '';
        dom.showBtn.classList.add('hidden');
        dom.ansContent.classList.add('hidden');
        dom.prev.disabled = true;
        dom.next.disabled = true;
        dom.progress.textContent = '0 / 0';
        return;
    }

    if (state.currentIndex >= filtered.length) state.currentIndex = filtered.length - 1;
    if (state.currentIndex < 0) state.currentIndex = 0;

    var q = filtered[state.currentIndex];
    var revealed = isRevealed(q);
    var scorable = isScorable(q);
    var userAns = state.answers[q.id];

    // Header
    dom.badge.textContent = TYPE_LABELS[q.type] || '未知';
    dom.badge.className = 'question-badge ' + (BADGE_CLASS[q.type] || '');
    dom.number.textContent = '第 ' + q.id + ' 题';

    // Question text
    dom.text.innerHTML = formatText(q.text);

    // Options
    if (q.type === 'single') {
        dom.options.innerHTML = buildSingleOpts(q, revealed, userAns);
    } else if (q.type === 'multiple') {
        dom.options.innerHTML = buildMultiOpts(q, revealed, userAns);
    } else if (q.type === 'tf') {
        dom.options.innerHTML = buildTfOpts(q, revealed, userAns);
    } else {
        dom.options.innerHTML = '';
    }

    // Answer button
    dom.showBtn.classList.remove('hidden');
    if (revealed) {
        dom.showBtn.textContent = scorable ? '已提交' : '已显示';
        dom.showBtn.disabled = true;
    } else if (scorable) {
        dom.showBtn.textContent = '提交答案';
        dom.showBtn.disabled = (userAns === undefined || userAns === '');
    } else {
        dom.showBtn.textContent = '显示答案与解析';
        dom.showBtn.disabled = false;
    }

    // Answer content
    if (revealed) {
        dom.ansContent.classList.remove('hidden');
        if (scorable) {
            var ok = checkCorrect(q);
            var prefix = ok ? '✓ 回答正确' : '✗ 回答错误';
            var color = ok ? 'var(--correct)' : 'var(--incorrect)';
            dom.ansText.innerHTML = '<span style="color:' + color + ';font-weight:700">' + prefix + '</span><br>' +
                '正确答案：' + esc(q.answer) +
                (userAns !== undefined ? '<br>你的答案：' + esc(userAns) : '');
        } else {
            dom.ansText.innerHTML = formatText(q.answer);
        }
        if (q.explanation) {
            dom.explArea.classList.remove('hidden');
            dom.explText.innerHTML = formatText(q.explanation);
        } else {
            dom.explArea.classList.add('hidden');
        }
    } else {
        dom.ansContent.classList.add('hidden');
        dom.explArea.classList.add('hidden');
    }

    // Navigation
    dom.prev.disabled = state.currentIndex === 0;
    dom.next.disabled = state.currentIndex >= filtered.length - 1;
    dom.progress.textContent = (state.currentIndex + 1) + ' / ' + filtered.length;

    updateGridActive();
    saveState();
}

function buildSingleOpts(q, revealed, userAns) {
    if (!q.options) return '';
    var h = '';
    q.options.forEach(function(o) {
        var c = 'option-card';
        if (revealed) {
            c += ' disabled';
            if (o.label === q.answer) c += ' correct-option';
            else if (o.label === userAns) c += ' incorrect-option';
        } else if (userAns === o.label) {
            c += ' selected';
        }
        h += '<div class="' + c + '" data-label="' + o.label + '">' +
            '<span class="option-label">' + o.label + '</span>' +
            '<span class="option-text">' + formatText(o.text) + '</span></div>';
    });
    return h;
}

function buildMultiOpts(q, revealed, userAns) {
    if (!q.options) return '';
    var sel = userAns ? userAns.split('') : [];
    var cor = q.answer.split('');
    var h = '';
    q.options.forEach(function(o) {
        var c = 'option-card';
        if (revealed) {
            c += ' disabled';
            if (cor.indexOf(o.label) !== -1) c += ' correct-option';
            else if (sel.indexOf(o.label) !== -1) c += ' incorrect-option';
        } else if (sel.indexOf(o.label) !== -1) {
            c += ' selected';
        }
        h += '<div class="' + c + '" data-label="' + o.label + '">' +
            '<span class="option-label">' + o.label + '</span>' +
            '<span class="option-text">' + formatText(o.text) + '</span></div>';
    });
    return h;
}

function buildTfOpts(q, revealed, userAns) {
    var items = [{ label: '正确', icon: '✓' }, { label: '错误', icon: '✗' }];
    var h = '<div class="tf-options">';
    items.forEach(function(it) {
        var c = 'tf-btn';
        if (revealed) {
            c += ' disabled';
            if (it.label === q.answer) c += ' correct-option';
            else if (it.label === userAns) c += ' incorrect-option';
        } else if (userAns === it.label) {
            c += ' selected';
        }
        h += '<button class="' + c + '" data-label="' + it.label + '">' +
            '<span class="tf-icon">' + it.icon + '</span> ' + it.label + '</button>';
    });
    h += '</div>';
    return h;
}

// ===== Stats =====
function updateStats() {
    var answered = 0, correct = 0, incorrect = 0;
    quizData.forEach(function(q) {
        if (!isRevealed(q)) return;
        answered++;
        if (isScorable(q)) {
            if (checkCorrect(q)) correct++;
            else incorrect++;
        }
    });
    dom.statAns.textContent = answered;
    dom.statTotal.textContent = quizData.length;
    dom.statOk.textContent = correct;
    dom.statWrong.textContent = incorrect;
    if (dom.sidebarAnswered) dom.sidebarAnswered.textContent = answered;
    if (dom.sidebarCorrect) dom.sidebarCorrect.textContent = correct;
    if (dom.sidebarIncorrect) dom.sidebarIncorrect.textContent = incorrect;
}

// ===== Actions =====
function selectOption(label) {
    var filtered = getFiltered();
    if (!filtered.length) return;
    var q = filtered[state.currentIndex];
    if (!q || isRevealed(q)) return;

    if (q.type === 'single' || q.type === 'tf') {
        state.answers[q.id] = label;
    } else if (q.type === 'multiple') {
        var cur = state.answers[q.id] || '';
        var idx = cur.indexOf(label);
        if (idx === -1) cur += label;
        else cur = cur.substring(0, idx) + cur.substring(idx + 1);
        cur = cur.split('').sort().join('');
        if (cur) state.answers[q.id] = cur;
        else delete state.answers[q.id];
    }
    renderQuestion();
}

function revealAnswer() {
    var filtered = getFiltered();
    if (!filtered.length) return;
    var q = filtered[state.currentIndex];
    if (!q || isRevealed(q)) return;
    state.revealed.push(q.id);
    renderQuestion();
    renderGrid();
    updateStats();

    // Auto-jump to next question on correct answer (scorable types)
    if (isScorable(q) && checkCorrect(q) && state.currentIndex < filtered.length - 1) {
        setTimeout(function() {
            goNext();
        }, 1200);
    }
}

function goTo(idx) {
    var filtered = getFiltered();
    if (idx < 0 || idx >= filtered.length) return;
    state.currentIndex = idx;
    renderQuestion();
}

function goPrev() { goTo(state.currentIndex - 1); }
function goNext() { goTo(state.currentIndex + 1); }

function setFilter(f) {
    state.filter = f;
    state.currentIndex = 0;
    dom.filters.querySelectorAll('button').forEach(function(btn) {
        btn.classList.toggle('active', btn.dataset.filter === f);
    });
    renderGrid();
    renderQuestion();
    saveState();
}

function viewStats() {
    var answered = 0, correct = 0, incorrect = 0, viewed = 0;
    quizData.forEach(function(q) {
        if (!isRevealed(q)) return;
        answered++;
        if (isScorable(q)) {
            if (checkCorrect(q)) correct++;
            else incorrect++;
        } else {
            viewed++;
        }
    });
    var scorableTotal = quizData.filter(function(q) { return isScorable(q); }).length;
    var pct = scorableTotal > 0 ? Math.round(correct / scorableTotal * 100) : 0;

    dom.summary.innerHTML =
        '<div class="result-score">' + pct + '%</div>' +
        '<div class="result-detail">' +
        '<div class="result-item"><div class="label">已答题目</div><div class="value">' + answered + ' / ' + quizData.length + '</div></div>' +
        '<div class="result-item correct-item"><div class="label">回答正确</div><div class="value">' + correct + '</div></div>' +
        '<div class="result-item incorrect-item"><div class="label">回答错误</div><div class="value">' + incorrect + '</div></div>' +
        '<div class="result-item"><div class="label">已看非判分题</div><div class="value">' + viewed + '</div></div>' +
        '</div>';
    dom.modal.classList.remove('hidden');
}

function restart() {
    state.answers = {};
    state.revealed = [];
    state.currentIndex = 0;
    state.filter = 'all';
    state.submitted = false;
    dom.filters.querySelectorAll('button').forEach(function(btn) {
        btn.classList.toggle('active', btn.dataset.filter === 'all');
    });
    renderGrid();
    renderQuestion();
    updateStats();
    saveState();
    dom.modal.classList.add('hidden');
}

// ===== Persistence =====
function saveState() {
    try {
        localStorage.setItem('quiz_state', JSON.stringify({
            currentIndex: state.currentIndex,
            answers: state.answers,
            revealed: state.revealed,
            filter: state.filter,
            submitted: state.submitted
        }));
    } catch (e) {}
}

function loadState() {
    try {
        var s = localStorage.getItem('quiz_state');
        if (!s) return;
        var d = JSON.parse(s);
        state.currentIndex = d.currentIndex || 0;
        state.answers = d.answers || {};
        state.revealed = d.revealed || [];
        state.filter = d.filter || 'all';
        state.submitted = d.submitted || false;
    } catch (e) {}
}

// ===== Events =====
function bindEvents() {
    dom.options.addEventListener('click', function(e) {
        var el = e.target.closest('[data-label]');
        if (el) selectOption(el.dataset.label);
    });

    dom.showBtn.addEventListener('click', revealAnswer);
    dom.prev.addEventListener('click', goPrev);
    dom.next.addEventListener('click', goNext);

    dom.filters.addEventListener('click', function(e) {
        var btn = e.target.closest('button[data-filter]');
        if (btn) setFilter(btn.dataset.filter);
    });

    dom.grid.addEventListener('click', function(e) {
        var btn = e.target.closest('.grid-btn');
        if (!btn) return;
        var id = parseInt(btn.dataset.id);
        var filtered = getFiltered();
        for (var i = 0; i < filtered.length; i++) {
            if (filtered[i].id === id) { goTo(i); break; }
        }
    });

    dom.submitBtn.addEventListener('click', viewStats);
    dom.review.addEventListener('click', function() { dom.modal.classList.add('hidden'); });
    dom.restart.addEventListener('click', restart);
    dom.modal.querySelector('.modal-overlay').addEventListener('click', function() {
        dom.modal.classList.add('hidden');
    });

    document.addEventListener('keydown', function(e) {
        if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') return;
        if (e.key === 'ArrowLeft') { goPrev(); e.preventDefault(); }
        if (e.key === 'ArrowRight') { goNext(); e.preventDefault(); }
    });

    dom.sidebarToggle.addEventListener('click', function() {
        dom.sidebar.classList.toggle('open');
        if (dom.sidebarBackdrop) dom.sidebarBackdrop.classList.toggle('show', dom.sidebar.classList.contains('open'));
    });
    if (dom.sidebarBackdrop) {
        dom.sidebarBackdrop.addEventListener('click', function() {
            dom.sidebar.classList.remove('open');
            dom.sidebarBackdrop.classList.remove('show');
        });
    }
    dom.grid.addEventListener('click', function() {
        if (window.innerWidth <= 1024) dom.sidebar.classList.remove('open');
    });
}

// ===== Init =====
function init() {
    initDom();

    if (typeof RAW_MD === 'undefined') {
        dom.text.innerHTML = '<div class="error-message"><h3>无法加载题库</h3><p>题库数据未找到</p></div>';
        return;
    }

    quizData = parseQuestions(RAW_MD);
    if (quizData.length === 0) {
        dom.text.innerHTML = '<div class="error-message"><h3>无法加载题库</h3><p>没有解析到任何题目</p></div>';
        return;
    }

    loadState();

    dom.filters.querySelectorAll('button').forEach(function(btn) {
        btn.classList.toggle('active', btn.dataset.filter === state.filter);
    });

    renderGrid();
    renderQuestion();
    updateStats();
    bindEvents();
}

init();
})();
